# Dépendances Python du pipeline.
# Le rendu (étape 3) génère les diagrammes localement avec Pillow — aucun
# accès réseau requis. Le montage (étape 4) appelle directement le binaire
# ffmpeg (plus robuste et sans dépendance à ImageMagick que moviepy pour un
# pipeline 100% automatisé).
openai-whisper
Pillow
